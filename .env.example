# Example environment file. Copy to `.env` (which is git-ignored) and fill in real
# values LOCALLY. Never commit a real .env. The starter apps do not require any of
# these — they exist to demonstrate the pattern for stretch goals.

# Example only — not used by the starter apps.
# API_BASE_URL=https://example.invalid/api
# FEATURE_FLAG_RECENT_SEARCHES=false
